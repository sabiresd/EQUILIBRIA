fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Base de donnees : motor pour un vrai MongoDB, mongomock-motor pour le mode
# memoire (MONGO_URL=memory) qui permet de lancer le projet sans rien installer.
motor==3.6.0
pymongo==4.9.2
mongomock-motor==0.0.34

# Certificats TLS : sans eux, une connexion Atlas (mongodb+srv) echoue sur
# Windows en 'SSL: CERTIFICATE_VERIFY_FAILED'. db.py l'importe en optionnel,
# mais en pratique c'est indispensable des qu'on quitte le mode memoire.
certifi==2024.12.14

# Lecture de Data2023.xlsx par le moteur (agent_simulation.load_dataset).
# Sans lui, tout run echoue : "openpyxl n'est pas installe".
openpyxl==3.1.5

pyjwt==2.10.1
bcrypt==4.2.1
httpx==0.28.1
slowapi==0.1.9
apscheduler==3.11.0
reportlab==4.2.5
email-validator==2.2.0

# tests
pytest==8.3.4
pytest-asyncio==0.25.0
